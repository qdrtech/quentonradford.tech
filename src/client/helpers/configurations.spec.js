import React from 'react';
import ReactDOM from 'react-dom';
import Configurations from './configurations';

xit('contains bing url', () => {
    let configurations = new Configurations();
    expect(configurations.BING_URL);
});
